<?php
/**	op-module-counter:/init.phtml
 *
 * @created   2026-05-17
 * @license   Apache-2.0
 * @package   op-module-counter
 * @author    Codex CLI
 * @copyright (C) 2026 Tomoaki Nagahara
 */

/* @var $issues  array   */
/* @var $process array   */
/* @var $chown   string  */
?>
<div class="op-module-counter-init" data-translate="true">
	<p>Access counter storage is not ready.</p>
	<ul>
		<?php foreach( $issues as $issue ): ?>
			<li><?= htmlspecialchars($issue, ENT_QUOTES, 'UTF-8') ?></li>
		<?php endforeach ?>
	</ul>
	<p>From the repository root, create `asset/db/` and make it writable by PHP.</p>
	<pre><code>mkdir -p asset/db/counter
chmod 775 asset/db asset/db/counter</code></pre>
	<p>This page is running under the following PHP user and group.</p>
	<pre><code>user: <?= htmlspecialchars($process['user_label'], ENT_QUOTES, 'UTF-8') . "\n" ?>
group: <?= htmlspecialchars($process['group_label'], ENT_QUOTES, 'UTF-8') ?></code></pre>
	<p>If `chmod` does not fix the issue, make `asset/db/` owned by the PHP process user.</p>
	<pre><code><?= htmlspecialchars($chown, ENT_QUOTES, 'UTF-8') ?></code></pre>
</div>
